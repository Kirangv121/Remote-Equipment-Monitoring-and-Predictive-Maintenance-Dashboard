// Middleware is disabled to prevent navigation issues
export function middleware() {
  // This middleware is intentionally empty to avoid interfering with client-side navigation
  return
}

export const config = {
  matcher: [],
}

